[project]
name = "agy-subag"
version = "0.1.0"
description = "Local MCP bridge for the agy scout agent"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = true

[project.scripts]
agy-mcp = "agy_mcp.server:main"
