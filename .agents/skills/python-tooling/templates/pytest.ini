# Minimal pytest config for new projects without one.
# Copy to project root if missing.

[pytest]
testpaths = test
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
