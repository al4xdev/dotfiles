# Minimal ruff config for new projects without one.
# Copy to project root if missing.

target-version = "py312"
line-length = 100

[lint]
select = ["E", "F", "I", "N", "W", "UP", "B", "C4", "SIM"]
ignore = []

[format]
quote-style = "double"
indent-style = "space"
